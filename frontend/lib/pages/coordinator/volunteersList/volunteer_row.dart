import 'package:flutter/material.dart';

class VolunteerRow extends StatelessWidget {
  const VolunteerRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('volunteerRow');
  }
}
